name: CD (Production)

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: production
      concurrency_group: deploy-main
    secrets: inherit
