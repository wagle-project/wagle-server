name: CD (Staging)

on:
  push:
    branches: [ develop ]

jobs:
  deploy:
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: staging
      concurrency_group: deploy-develop
    secrets: inherit
